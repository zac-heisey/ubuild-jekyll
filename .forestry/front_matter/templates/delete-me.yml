---
label: DELETE ME
hide_body: true
fields:
- name: block
  type: blocks
  label: blockBISCH
  template_types:
  - simple-footer
  - 2-column-text
  config:
    min: 
    max: 
- name: colors
  type: color
  label: colors
  config:
    required: false
    color_format: Hex
